---
# GAD Infrastructure Ansible Inventory
# Converted from hosts.ini to YAML format with enhancements

all:
  children:
    webservers:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
          ansible_become_method: sudo
          ansible_become_flags: "--preserve-env"
        
        GADHQMSCAP02.cce3.gpc:
          ansible_host: GADHQMSCAP02.cce3.gpc
          ansible_user: "{{ vault_ansible_user | default('vincent.zamora') }}"
          ansible_become_method: sudo
          ansible_become_flags: "--preserve-env"
          
    windows_servers:
      hosts:
        # Example Windows server configuration
        # Uncomment and modify as needed
        # win-server-01:
        #   ansible_host: 192.168.1.100
        #   ansible_user: Administrator
        #   ansible_connection: winrm
        #   ansible_winrm_transport: basic
        #   ansible_winrm_server_cert_validation: ignore
        #   ansible_become_method: runas
        
    database:
      hosts:
        # Add database servers here
        # db-server-01:
        #   ansible_host: 192.168.1.200
        #   ansible_user: "{{ vault_db_user }}"
        
    network:
      hosts:
        # Add network devices here
        # switch-01:
        #   ansible_host: 192.168.1.10
        #   ansible_network_os: ios
        
    security:
      hosts:
        # Add security appliances here
        # firewall-01:
        #   ansible_host: 192.168.1.1 